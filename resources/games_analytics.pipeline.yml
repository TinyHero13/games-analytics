# The main pipeline for games_analytics
resources:
  pipelines:
    games_analytics_pipeline:
      name: games_analytics_pipeline
      catalog: workspace
      schema: games_analytics_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
