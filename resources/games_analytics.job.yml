resources:
  pipelines:
    games_analytics_pipeline:
      name: games_analytics_pipeline
      
      catalog: games_analytics
      target: dev 
      
      continuous: false

      environment:
        dependencies: ["${workspace.artifact_path}/.internal/*.whl"]
            
      libraries:
        - file:
            path: ../src/games_pipeline.py

      serverless: true