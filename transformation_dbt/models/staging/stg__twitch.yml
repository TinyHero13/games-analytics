version: 2

models:
  - name: stg__twitch
    description: "Top 100 jogos mais assistidos no Twitch"
    columns:
      - name: twitch_id
        description: "ID único no dataset Twitch"
        tests:
          - unique
          - not_null
      - name: igdb_id
        description: "ID correspondente no IGDB (quando encontrado)"
      - name: game_name
        description: "Nome do jogo no Twitch"
        tests:
          - not_null
      - name: box_art_url
        description: "URL da imagem do jogo"
