version: 2

models:
  - name: stg__languages
    description: "Idiomas suportados pelos jogos"
    columns:
      - name: igdb_id
        description: "ID do jogo no IGDB"
        tests:
          - not_null
      - name: language_id
        description: "ID único do idioma"
        tests:
          - not_null
      - name: language_name
        description: "Nome do idioma"
        tests:
          - not_null
      - name: language_support_type
        description: "Tipo de suporte (audio, subtitles, interface)"
