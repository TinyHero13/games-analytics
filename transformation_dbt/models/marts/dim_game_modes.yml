version: 2

models:
  - name: dim_game_modes
    description: "Game modes dimension"
    columns:
      - name: game_mode_key
        description: "Dimension primary key"
        tests:
          - unique
          - not_null
      - name: game_mode_name
        description: "Game mode name"
        tests:
          - not_null
      - name: updated_at
        description: "Last update date"
        tests:
          - not_null
